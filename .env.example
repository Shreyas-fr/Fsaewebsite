# Backend API URL — set this in Vercel Dashboard > Settings > Environment Variables
# Deploy your server/ folder to Render or Railway, then paste the URL here.
VITE_API_URL=https://your-backend.onrender.com
